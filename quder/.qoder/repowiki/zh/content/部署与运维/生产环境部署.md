# 生产环境部署

<cite>
**本文档引用的文件**
- [pom.xml](file://backend/pom.xml)
- [application.yml](file://backend/src/main/resources/application.yml)
- [DemoApplication.java](file://backend/src/main/java/com/example/demo/DemoApplication.java)
- [UserController.java](file://backend/src/main/java/com/example/demo/controller/UserController.java)
- [UserService.java](file://backend/src/main/java/com/example/demo/service/UserService.java)
- [package.json](file://frontend/package.json)
- [vite.config.ts](file://frontend/vite.config.ts)
- [user.ts](file://frontend/src/api/user.ts)
- [tsconfig.json](file://frontend/tsconfig.json)
- [tsconfig.node.json](file://frontend/tsconfig.node.json)
- [README.md](file://README.md)
</cite>

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构概览](#架构概览)
5. [详细组件分析](#详细组件分析)
6. [生产环境配置优化](#生产环境配置优化)
7. [Docker容器化部署](#docker容器化部署)
8. [云平台部署策略](#云平台部署策略)
9. [负载均衡与SSL配置](#负载均衡与ssl配置)
10. [监控告警实施](#监控告警实施)
11. [性能优化建议](#性能优化建议)
12. [故障排除指南](#故障排除指南)
13. [结论](#结论)

## 简介

本指南面向生产环境部署，针对基于Spring Boot 3.2.0 + Vue 3的全栈项目提供完整的部署解决方案。项目采用前后端分离架构，后端使用Java 21和Spring Boot 3.x，前端使用Vue 3 + TypeScript + Vite构建工具链。

项目的核心功能包括RESTful API接口、CORS跨域支持、用户管理功能等。本文档将详细说明从代码构建到生产部署的完整流程，包括容器化、云平台部署、负载均衡、SSL证书配置和监控告警等企业级部署实践。

## 项目结构

该项目采用标准的前后端分离架构，具有清晰的模块划分和职责分离：

```mermaid
graph TB
subgraph "后端服务 (Spring Boot)"
BA[DemoApplication.java<br/>应用入口]
BC[controller/<br/>REST控制器]
BS[service/<br/>业务逻辑层]
BM[model/<br/>数据模型]
BR[resources/<br/>配置文件]
end
subgraph "前端应用 (Vue 3)"
FA[src/api/<br/>API封装]
FC[src/components/<br/>Vue组件]
FV[src/views/<br/>页面视图]
FCFG[vite.config.ts<br/>构建配置]
FP[package.json<br/>依赖管理]
end
subgraph "构建工具"
MP[Maven<br/>后端构建]
VP[Vite<br/>前端构建]
TP[TypeScript<br/>类型检查]
end
BA --> BC
BA --> BS
BA --> BM
BC --> BS
BS --> BM
FA --> FV
FC --> FV
FCFG --> VP
FP --> VP
MP --> BA
VP --> FV
TP --> FA
```

**图表来源**
- [DemoApplication.java:1-13](file://backend/src/main/java/com/example/demo/DemoApplication.java#L1-L13)
- [UserController.java:1-30](file://backend/src/main/java/com/example/demo/controller/UserController.java#L1-L30)
- [UserService.java:1-33](file://backend/src/main/java/com/example/demo/service/UserService.java#L1-L33)
- [vite.config.ts:1-23](file://frontend/vite.config.ts#L1-L23)

**章节来源**
- [README.md:5-30](file://README.md#L5-L30)
- [pom.xml:1-48](file://backend/pom.xml#L1-L48)
- [package.json:1-24](file://frontend/package.json#L1-L24)

## 核心组件

### 后端核心组件

后端采用Spring Boot框架，主要组件包括应用入口、REST控制器和业务服务层：

```mermaid
classDiagram
class DemoApplication {
+main(args) void
}
class UserController {
-userService UserService
+getAllUsers() User[]
+addUser(user) User
}
class UserService {
-users User[]
-idCounter AtomicLong
+getAllUsers() User[]
+addUser(user) User
}
class User {
+id Long
+name String
+email String
}
DemoApplication --> UserController : "启动时加载"
UserController --> UserService : "依赖注入"
UserService --> User : "操作数据"
UserController --> User : "返回数据"
```

**图表来源**
- [DemoApplication.java:1-13](file://backend/src/main/java/com/example/demo/DemoApplication.java#L1-L13)
- [UserController.java:1-30](file://backend/src/main/java/com/example/demo/controller/UserController.java#L1-L30)
- [UserService.java:1-33](file://backend/src/main/java/com/example/demo/service/UserService.java#L1-L33)

### 前端核心组件

前端采用Vue 3 Composition API，主要组件包括API封装、组件系统和构建配置：

```mermaid
classDiagram
class UserApi {
+getUsers() Promise
+addUser(user) Promise
}
class ApiClient {
+baseURL String
+timeout Number
+headers Object
}
class User {
+id Number
+name String
+email String
}
UserApi --> ApiClient : "使用"
UserApi --> User : "处理数据"
```

**图表来源**
- [user.ts:1-26](file://frontend/src/api/user.ts#L1-L26)

**章节来源**
- [DemoApplication.java:1-13](file://backend/src/main/java/com/example/demo/DemoApplication.java#L1-L13)
- [UserController.java:1-30](file://backend/src/main/java/com/example/demo/controller/UserController.java#L1-L30)
- [UserService.java:1-33](file://backend/src/main/java/com/example/demo/service/UserService.java#L1-L33)
- [user.ts:1-26](file://frontend/src/api/user.ts#L1-L26)

## 架构概览

项目采用经典的三层架构模式，前后端完全分离：

```mermaid
graph TB
subgraph "客户端层"
Browser[Web浏览器]
Mobile[移动应用]
end
subgraph "网关层"
Nginx[Nginx反向代理]
LB[负载均衡器]
end
subgraph "应用层"
Frontend[Vue 3前端应用]
Backend[Spring Boot后端服务]
end
subgraph "数据层"
Database[(数据库)]
Redis[(缓存)]
end
Browser --> Nginx
Mobile --> Nginx
Nginx --> LB
LB --> Frontend
LB --> Backend
Frontend --> Backend
Backend --> Database
Backend --> Redis
```

**图表来源**
- [application.yml:1-13](file://backend/src/main/resources/application.yml#L1-L13)
- [vite.config.ts:13-21](file://frontend/vite.config.ts#L13-L21)

## 详细组件分析

### 后端应用配置分析

后端应用配置相对简洁，主要包含服务器端口、应用名称和日志级别设置：

```mermaid
flowchart TD
Start([应用启动]) --> LoadConfig["加载application.yml配置"]
LoadConfig --> ParseServer["解析服务器配置<br/>端口: 8080"]
ParseServer --> ParseSpring["解析Spring配置<br/>应用名称: demo-backend"]
ParseSpring --> ParseLogging["解析日志配置<br/>DEBUG级别"]
ParseLogging --> InitComponents["初始化组件"]
InitComponents --> Ready([应用就绪])
```

**图表来源**
- [application.yml:1-13](file://backend/src/main/resources/application.yml#L1-L13)

**章节来源**
- [application.yml:1-13](file://backend/src/main/resources/application.yml#L1-L13)

### 前端构建流程分析

前端采用Vite作为构建工具，支持热重载和快速构建：

```mermaid
sequenceDiagram
participant Dev as "开发者"
participant Vite as "Vite构建器"
participant TS as "TypeScript编译器"
participant Bundle as "打包器"
participant Output as "输出目录"
Dev->>Vite : 执行npm run build
Vite->>TS : 编译TypeScript文件
TS-->>Vite : 编译完成
Vite->>Bundle : 打包Vue组件和静态资源
Bundle-->>Output : 生成dist目录
Output-->>Dev : 可部署的静态资源
```

**图表来源**
- [package.json:6-10](file://frontend/package.json#L6-L10)
- [vite.config.ts:1-23](file://frontend/vite.config.ts#L1-L23)

**章节来源**
- [package.json:1-24](file://frontend/package.json#L1-L24)
- [vite.config.ts:1-23](file://frontend/vite.config.ts#L1-L23)

## 生产环境配置优化

### 数据库连接配置

生产环境建议使用连接池和SSL加密：

```mermaid
flowchart TD
Config[数据库配置] --> Pool[连接池配置]
Config --> SSL[SSL证书配置]
Config --> Timeout[超时设置]
Pool --> MaxActive[最大连接数]
Pool --> MinIdle[最小空闲连接]
Pool --> MaxWait[最大等待时间]
SSL --> EnableSSL[启用SSL]
SSL --> TrustStore[信任存储]
SSL --> KeyStore[密钥存储]
Timeout --> ConnectTimeout[连接超时]
Timeout --> SocketTimeout[Socket超时]
```

**图表来源**
- [application.yml:4-13](file://backend/src/main/resources/application.yml#L4-L13)

### 日志配置优化

生产环境建议调整日志级别和输出格式：

| 日志级别 | 适用场景 | 建议配置 |
|---------|---------|---------|
| ERROR | 错误信息 | 生产默认级别 |
| WARN | 警告信息 | 关键警告 |
| INFO | 一般信息 | 应用状态 |
| DEBUG | 调试信息 | 仅开发环境 |

### 安全配置建议

```mermaid
flowchart LR
Security[安全配置] --> CORS[CORS配置]
Security --> HTTPS[HTTPS强制]
Security --> Auth[认证授权]
CORS --> Origin[允许源]
CORS --> Methods[允许方法]
CORS --> Headers[允许头]
HTTPS --> Cert[SSL证书]
HTTPS --> Redirect[HTTP重定向]
Auth --> JWT[JWT令牌]
Auth --> RBAC[基于角色权限]
```

**章节来源**
- [UserController.java:11](file://backend/src/main/java/com/example/demo/controller/UserController.java#L11)
- [application.yml:8-13](file://backend/src/main/resources/application.yml#L8-L13)

## Docker容器化部署

### Dockerfile编写

为Spring Boot应用创建Dockerfile：

```dockerfile
FROM openjdk:21-jre-slim

# 设置工作目录
WORKDIR /app

# 复制JAR文件
COPY target/demo-0.0.1-SNAPSHOT.jar app.jar

# 暴露端口
EXPOSE 8080

# 健康检查
HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
    CMD curl -f http://localhost:8080/actuator/health || exit 1

# 启动应用
ENTRYPOINT ["java", "-jar", "app.jar"]
```

为Vue前端创建Dockerfile：

```dockerfile
FROM node:20-alpine

# 设置工作目录
WORKDIR /app

# 复制依赖文件
COPY package*.json ./

# 安装依赖
RUN npm ci --only=production

# 复制源码
COPY . .

# 构建静态资源
RUN npm run build

# 使用Nginx运行
FROM nginx:alpine
COPY --from=0 /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/nginx.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

### Docker Compose配置

```yaml
version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/mydb
      - SPRING_DATASOURCE_USERNAME=user
      - SPRING_DATASOURCE_PASSWORD=pass
    depends_on:
      - mysql
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    environment:
      - VITE_API_BASE_URL=http://localhost/api

  mysql:
    image: mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=rootpass
      - MYSQL_DATABASE=mydb
      - MYSQL_USER=user
      - MYSQL_PASSWORD=pass
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]

volumes:
  db_data:
```

**章节来源**
- [pom.xml:39-46](file://backend/pom.xml#L39-L46)
- [package.json:6-10](file://frontend/package.json#L6-L10)

## 云平台部署策略

### AWS部署流程

```mermaid
flowchart TD
Plan[规划阶段] --> Build[构建镜像]
Build --> Deploy[部署到ECS]
Deploy --> Configure[配置负载均衡]
Configure --> Monitor[监控配置]
Plan --> EC2[EC2实例]
Plan --> ECS[ECS集群]
Plan --> EKS[EKS集群]
EC2 --> AMI[创建AMI]
ECS --> Task[任务定义]
EKS --> Pod[Pod配置]
AMI --> Launch[启动配置]
Task --> Service[服务配置]
Pod --> Deployment[部署配置]
Launch --> AutoScaling[自动扩展]
Service --> LoadBalancer[负载均衡器]
Deployment --> Ingress[Ingress控制器]
```

### Azure部署流程

Azure平台推荐使用Azure Container Instances或Azure Kubernetes Service：

```mermaid
sequenceDiagram
participant Dev as "开发者"
participant ACR as "Azure容器注册表"
participant AKS as "AKS集群"
participant LB as "Azure负载均衡器"
Dev->>ACR : 推送Docker镜像
ACR-->>AKS : 拉取镜像
AKS->>LB : 配置服务暴露
LB-->>Dev : 提供外部访问
```

### 阿里云部署流程

阿里云推荐使用容器服务Kubernetes版（ACK）：

```mermaid
flowchart LR
Git[Git仓库] --> Jenkins[Jenkins流水线]
Jenkins --> Docker[Docker镜像构建]
Docker --> Registry[镜像仓库]
Registry --> ACK[ACK集群]
ACK --> SLB[SLB负载均衡]
SLB --> Users[用户访问]
```

## 负载均衡与SSL配置

### Nginx负载均衡配置

```nginx
upstream backend {
    server backend-1:8080;
    server backend-2:8080;
    server backend-3:8080;
}

upstream frontend {
    server frontend-1:80;
    server frontend-2:80;
    server frontend-3:80;
}

server {
    listen 80;
    server_name example.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name example.com;
    
    ssl_certificate /path/to/certificate.crt;
    ssl_certificate_key /path/to/private.key;
    
    location / {
        proxy_pass http://frontend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    location /api/ {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

### SSL证书配置

```mermaid
flowchart TD
Certificate[SSL证书] --> Let'sEncrypt[Let's Encrypt]
Certificate --> Commercial[商业证书]
Let'sEncrypt --> AutoRenew[自动续期]
Commercial --> ManualRenew[手动续期]
AutoRenew --> Certbot[Certbot工具]
ManualRenew --> CA[证书颁发机构]
Certbot --> Nginx[Nginx配置]
CA --> Nginx
```

**章节来源**
- [vite.config.ts:15-20](file://frontend/vite.config.ts#L15-L20)

## 监控告警实施

### 应用监控指标

```mermaid
graph TB
subgraph "应用指标"
CPU[CPU使用率]
Memory[内存使用量]
Requests[请求量]
Latency[响应延迟]
Errors[错误率]
end
subgraph "基础设施指标"
Network[网络流量]
Disk[磁盘空间]
Load[系统负载]
end
subgraph "业务指标"
Users[活跃用户]
Transactions[事务数量]
Revenue[收入统计]
end
CPU --> Monitoring[监控系统]
Memory --> Monitoring
Requests --> Monitoring
Latency --> Monitoring
Errors --> Monitoring
Network --> Monitoring
Disk --> Monitoring
Load --> Monitoring
Users --> Monitoring
Transactions --> Monitoring
Revenue --> Monitoring
```

### 告警配置策略

| 指标类型 | 告警阈值 | 告警级别 | 告警方式 |
|---------|---------|---------|---------|
| CPU使用率 | >90% | 危险 | 邮件/SMS |
| 内存使用率 | >85% | 危险 | 邮件/SMS |
| 响应延迟 | >2秒 | 警告 | 钉钉/微信 |
| 错误率 | >5% | 危险 | 邮件/SMS |
| 请求量 | 异常波动 | 警告 | 钉钉/微信 |

## 性能优化建议

### Spring Boot性能优化

```mermaid
flowchart TD
Optimize[性能优化] --> JVM[JVM调优]
Optimize --> Database[数据库优化]
Optimize --> Caching[缓存策略]
Optimize --> Async[异步处理]
JVM --> Heap[堆内存大小]
JVM --> GC[垃圾回收器]
JVM --> Thread[线程池配置]
Database --> Connection[连接池优化]
Database --> Query[查询优化]
Database --> Index[索引优化]
Caching --> Redis[Redis配置]
Caching --> Local[本地缓存]
Async --> ThreadPool[线程池配置]
Async --> MessageQueue[消息队列]
```

### 前端性能优化

```mermaid
flowchart LR
Performance[前端性能] --> Bundle[代码分割]
Performance --> Lazy[懒加载]
Performance --> CDN[CDN加速]
Performance --> Compression[压缩优化]
Bundle --> Split[路由分割]
Bundle --> Component[组件分割]
Lazy --> Route[路由懒加载]
Lazy --> ComponentLazy[组件懒加载]
CDN --> Static[静态资源CDN]
CDN --> Fonts[字体CDN]
Compression --> Gzip[Gzip压缩]
Compression --> Brotli[Brotli压缩]
```

### 资源调优参数

| 参数类别 | 参数名称 | 建议值 | 说明 |
|---------|---------|--------|------|
| JVM | Xms | 512m | 初始堆大小 |
| JVM | Xmx | 2g | 最大堆大小 |
| JVM | XX:MaxRAMPercentage | 75.0 | 最大内存百分比 |
| 数据库 | maxPoolSize | 20 | 连接池大小 |
| 数据库 | connectionTimeout | 30000 | 连接超时(ms) |
| 数据库 | idleTimeout | 600000 | 空闲超时(ms) |
| 缓存 | redis.maxTotal | 100 | 连接池大小 |
| 缓存 | redis.maxWaitMillis | 1000 | 等待超时(ms) |

## 故障排除指南

### 常见问题诊断

```mermaid
flowchart TD
Issue[问题出现] --> Log[查看日志]
Log --> Error[错误信息]
Error --> RootCause[根因分析]
RootCause --> Solution[解决方案]
Log --> ApplicationLog[应用日志]
Log --> SystemLog[系统日志]
Log --> NetworkLog[网络日志]
Error --> HTTPError[HTTP错误]
Error --> DatabaseError[数据库错误]
Error --> NetworkError[网络错误]
HTTPError --> Status4xx[4xx错误]
HTTPError --> Status5xx[5xx错误]
Status4xx --> ClientError[客户端错误]
Status5xx --> ServerError[服务器错误]
```

### 排查步骤

1. **日志分析**
   - 检查应用启动日志
   - 分析错误堆栈信息
   - 查看数据库连接日志

2. **网络诊断**
   - 测试端口连通性
   - 检查防火墙规则
   - 验证DNS解析

3. **资源监控**
   - 监控CPU使用率
   - 检查内存泄漏
   - 分析磁盘空间

### 性能问题定位

```mermaid
flowchart TD
PerformanceIssue[性能问题] --> Identify[问题识别]
Identify --> Profile[性能分析]
Profile --> Optimize[优化实施]
Optimize --> Verify[效果验证]
Identify --> SlowResponse[响应缓慢]
Identify --> HighCPU[CPU过高]
Identify --> MemoryLeak[内存泄漏]
Profile --> JProfiler[JProfiler分析]
Profile --> GCLog[GC日志分析]
Profile --> Database[数据库分析]
Optimize --> Code[代码优化]
Optimize --> Config[配置优化]
Optimize --> Infrastructure[基础设施优化]
```

**章节来源**
- [application.yml:8-13](file://backend/src/main/resources/application.yml#L8-L13)
- [UserController.java:11](file://backend/src/main/java/com/example/demo/controller/UserController.java#L11)

## 结论

本指南提供了从开发到生产的完整部署路径，涵盖了现代Web应用的各个方面。通过采用容器化、微服务架构和云原生技术，可以实现高可用、高性能的应用部署。

关键要点包括：
- 使用Docker进行容器化部署，确保环境一致性
- 实施CI/CD流水线，自动化测试和部署
- 配置负载均衡和SSL证书，确保高可用性和安全性
- 建立完善的监控告警体系，及时发现和解决问题
- 持续优化性能参数，提升用户体验

建议在实际部署前进行充分的测试和验证，确保生产环境的稳定性和可靠性。